import React from "react";
import "../css/App/header.css"

const Header = () => {
return (
    <div className="header-title">
        <h1 className="header-text">Lists of the table</h1>
    </div>
);
};

export default Header;